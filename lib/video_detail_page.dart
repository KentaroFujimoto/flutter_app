import 'package:flutter/material.dart';

class VideoDetalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(
        child: Center(
            child: Text('動画')
        ),
      ),
    );
  }
}